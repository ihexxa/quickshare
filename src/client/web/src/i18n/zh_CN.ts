import { Map } from "immutable";

const adContent = `
发现bug等, 可至下方github库提交issue, 不过周末可能不上班😎.`;

export const msgs: Map<string, string> = Map({
  "loader.top.about": "关于",
});